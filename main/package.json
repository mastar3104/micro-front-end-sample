{
  "name": "micro-front-end-sample",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf .parcel-cache && rm -rf ../docs/*.html && rm -rf ../docs/*.js && parcel build --dist-dir ../docs ./index.html --public-url ./"
  },
  "author": "mastar3104",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.9.3",
    "process": "^0.11.10",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
